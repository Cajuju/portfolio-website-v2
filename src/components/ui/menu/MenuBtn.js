import React from 'react';
import './MenuBtn.scss';

const menuBtn = () => {
    return (
        <button className="btn-custom">
            <i className="fas fa-bars"></i>
        </button> 
    );
};

export default menuBtn;